package gen

// A module that declares source generators: the transpile has to be rejected by name
// (specs/simse-md.md), so nothing here is ever compiled.

fun main(): Int {
    println(1)
    return 0
}
