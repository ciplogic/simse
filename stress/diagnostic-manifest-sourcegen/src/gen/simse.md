sourcegen: true

// This module ships source generators (specs/simse-md.md): using it would need a compiler
// extended with them, which the compiler cannot do yet - so the compilation is dropped
// rather than compiled without them.
