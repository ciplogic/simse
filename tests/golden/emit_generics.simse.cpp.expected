// Generated by simse_transpile. Do not edit.
#include "cppsrc/rtl/simse.hpp"
#include <iostream>
#include <type_traits>

// emit_generics.simse:2
SIMSE_PACK_PUSH
template <class A, class B>
struct Pair {
    A first;
    B second;
};
SIMSE_PACK_POP
template <class A, class B>
Pair<A, B> _make_Pair(A first, B second) {
    return Pair<A, B>{first, second};
}
template <class T>
T identity(T value);
// emit_generics.simse:4
template <class T>
T identity(T value) {
    return value;
}
// emit_generics.simse:8
int main() {
    List<Int> numbers = List<Int>();
    SmallVector<Int, 4> vector = SmallVector<Int, 4>();
    Pair<Int, Bool> a = _make_Pair<Int, Bool>(1, true);
    Pair<Str, Int> b = _make_Pair<Str, Int>("two", 2);
    std::cout << std::boolalpha << (identity<Int>(7)) << std::endl;
    std::cout << std::boolalpha << (a.first) << std::endl;
    std::cout << std::boolalpha << (b.second) << std::endl;
    std::cout << std::boolalpha << (numbers.size()) << std::endl;
    return 0;
}
