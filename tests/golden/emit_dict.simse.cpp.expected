// Generated by simse_transpile. Do not edit.
#include "cppsrc/rtl/simse.hpp"
#include <iostream>
#include <type_traits>

// emit_dict.simse:4
int main() {
    Dictionary<Str, Int> counts = simse_dictionaryOf<Str, Int>();
    simse_dict_insert(counts, "b", 2);
    simse_dict_insert(counts, "a", 1);
    simse_dict_insert(counts, "c", 3);
    simse_dict_insert(counts, "b", 20);
    std::cout << std::boolalpha << (simse_dict_size(counts)) << std::endl;
    std::cout << std::boolalpha << (simse_dict_get(counts, "a").value()) << std::endl;
    std::cout << std::boolalpha << (simse_dict_has(counts, "z")) << std::endl;
    List<Str> names = simse_dict_keys(counts);
    simse_list_sort(names, [=](Str left, Str right) -> Bool { return left < right; });
    Int i = 0;
    while (i < names.size()) {
        std::cout << std::boolalpha << (names[i]) << std::endl;
        i = i + 1;
    }
    std::cout << std::boolalpha << (simse_list_contains(names, "c")) << std::endl;
    std::cout << std::boolalpha << (simse_list_contains(names, "z")) << std::endl;
    List<Int> values = simse_dict_values(counts);
    simse_list_sort(values, [=](Int left, Int right) -> Bool { return left < right; });
    Int j = 0;
    while (j < values.size()) {
        std::cout << std::boolalpha << (values[j]) << std::endl;
        j = j + 1;
    }
    simse_dict_remove(counts, "a");
    std::cout << std::boolalpha << (simse_dict_size(counts)) << std::endl;
    simse_dict_clear(counts);
    std::cout << std::boolalpha << (simse_dict_size(counts)) << std::endl;
    return 0;
}
