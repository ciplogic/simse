// Generated by simse_transpile. Do not edit.
#include "cppsrc/rtl/simse.hpp"
#include <iostream>
#include <type_traits>

Int sum(std::shared_ptr<List<Int>> items);
Int headSum(std::shared_ptr<List<Int>> items);
// emit_cursor.simse:3
Int sum(std::shared_ptr<List<Int>> items) {
    Int total = 0;
    Cursor<Int> c = simse_cursorOf(items);
    while (c.hasValue()) {
        total = total + c.value();
        c = c.next();
    }
    return total;
}
// emit_cursor.simse:13
Int headSum(std::shared_ptr<List<Int>> items) {
    Cursor<Int> full = simse_cursorOf(items);
    Cursor<Int> head = full.slice(2);
    Int total = 0;
    while (head.hasValue()) {
        total = total + head.value();
        head = head.next();
    }
    return total;
}
// emit_cursor.simse:24
int main() {
    List<Int> items = List<Int>();
    simse_list_append(items, 4);
    simse_list_append(items, 7);
    simse_list_append(items, 9);
    std::shared_ptr<List<Int>> view = std::make_shared<std::remove_cvref_t<decltype((items))>>(items);
    std::cout << std::boolalpha << (sum(view)) << std::endl;
    Cursor<Int> all = simse_cursorOf(view);
    std::cout << std::boolalpha << (all.size()) << std::endl;
    std::cout << std::boolalpha << (headSum(view)) << std::endl;
    return 0;
}
