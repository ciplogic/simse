// Negative sema fixture: `List<T>.append` is a prelude extension taking one
// value argument, so calling it with none is an extension-arity error. This
// exercises the path that requires the RTL prelude to be in scope.

fun main(): Int {
    var xs: List<Int> = List<Int>()
    xs.append()
    return 0
}
